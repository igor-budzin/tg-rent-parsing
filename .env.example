# Telegram API credentials
# Get these from https://my.telegram.org/apps
API_ID=your_api_id_here
API_HASH=your_api_hash_here

# Your Telegram username (without @) or user ID to receive notifications
# Used when NOT using bot notifications
NOTIFY_USER=your_username

# ===== BOT NOTIFICATIONS (RECOMMENDED) =====
# To receive real DM notifications from a bot instead of "Saved Messages":
# 1. Create a bot via @BotFather on Telegram
# 2. Get your bot token and add it below
# 3. Get your user ID from @userinfobot and add it below
# 4. IMPORTANT: Start a chat with your bot first (send /start to your bot)
BOT_TOKEN=
TELEGRAM_USER_ID=

# Authentication method: "qr" (default) or "phone"
# QR: Scan QR code with Telegram app (Settings -> Devices -> Link Desktop Device)
# Phone: Enter phone number and receive SMS/Telegram code
AUTH_METHOD=qr

# Check interval in milliseconds (optional, default: 60000)
CHECK_INTERVAL_MS=60000
